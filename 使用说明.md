# 晚安吻 — 使用说明

伴我入睡，文字和月光。

---

## 产品简介

晚安吻是一款本地局域网听书服务，对接 GPT-SoVITS 语音合成引擎，可以用定制的声音为你朗读书籍。支持播客模式和听书模式两种体验，支持手机端扫码使用。

---

## 功能一览

### 书架

- **放置 TXT 书籍**：上传的书籍会显示在书架
- **删除 TXT 书籍**：可删除已上传的书籍
- **双模式播放**：可选择播客模式（沉浸式背景 + 滚动字幕）与听书模式（纯文本阅读）
- **章节跳转**：系统会自动识别书籍章节结构（支持中英文书籍）并进行切分，用户可自由点播
- **音频缓存**：首次加载需等待合成，合成结果自动缓存，第二次加载无需等待，引擎离线后已合成的音频仍可播放
- **进度条**：显示已听进度，支持自由拖拽跳转
- **连播**：开启连播功能，自动播放下一章

### 历史

- **历史记录**：查看已听过的书籍和进度
- **进度读取**：自动保存收听进度，支持续听

### 配置

- **声音模型配置**：上传 GPT 权重、SoVITS 权重和参考音频，在线声音定制
- **上传书籍**：上传 TXT 书籍
- **播客背景图**：自定义播客模式的背景图片
- **TTS 引擎配置**：绑定 GPT-SoVITS 引擎
- **TTS 引擎管理**：在网页内一键启动/关闭 GPT-SoVITS 引擎

### 其他

- **局域网二维码**：位于右上角，手机扫码即可在移动端听书
- **状态**：实时监测引擎状态

---

## 快速上手

### 第一步：启动程序

双击桌面的「晚安吻」图标（或双击 `run.bat`），浏览器会自动打开 http://localhost:5200

### 第二步：上传声音模型

1. 上传 **GPT 权重** 文件（.ckpt）
2. 上传 **SoVITS 权重** 文件（.ckpt）
3. 上传 **参考音频**（.wav）并填写参考音频文本
4. 点击 **「保存」**

### 第三步：配置引擎

1. 点击底部 **「配置」** 
2. 填写 **引擎目录**（GPT-SoVITS 所在文件夹路径）
3. 点击 **「保存」** → **「启动引擎」** → **「测试连接」**
4. 等待右上角显示「引擎就绪」

### 第四步：上传书籍并开始听书

1. 在配置页上传 TXT 书籍和播客背景图
2. 回到 **「书架」**，选择书籍
3. 选择 **「播客模式」** 或 **「听书模式」**
4. 点击播放，开始享受定制阅读！

---

## 配置项说明

晚安吻共有六项配置：

| 配置项 | 说明 | 必填 |
| --- | --- | --- |
| TTS 引擎 | GPT-SoVITS 引擎目录，用于启动语音合成服务 | 是 |
| GPT 权重 | 训练好的 GPT 模型文件（.ckpt / .pth） | 是 |
| SoVITS 权重 | 训练好的 SoVITS 模型文件（.ckpt / .pth） | 是 |
| 参考音频 | 3~15 秒的 .wav 音频，作为声音克隆的样本 | 是 |
| 上传书籍 | TXT 格式的电子书，支持中英文 | 是 |
| 播客背景图 | 播客模式的背景图片（.jpg / .png） | 否 |

---

## 注意事项

### 引擎相关

- 引擎目录路径**不要包含中文**，建议使用纯英文路径
- 启动引擎后需等待 10~20 秒，引擎初始化需要时间
- 如果端口 9880 被占用，请先关闭占用该端口的程序，或重启电脑
- 杀毒软件可能会误报 GPT-SoVITS 引擎文件，请手动允许

### 声音模型

- GPT 权重和 SoVITS 权重必须来自**同一次训练**，不要混用不同版本的模型
- 首次合成所需时间较长，请耐心等待
- 更换声音模型后，已合成的音频缓存会自动清除，需要重新合成
- 参考音频文本必须与参考音频中实际说的内容一致

### 书籍上传

- 仅支持 **TXT** 格式，支持 UTF-8 / GBK 等常见编码
- 文件大小建议不超过 10MB
- 中英文书籍均支持自动章节识别
- 书籍删除后，已合成的音频缓存会自动清除，再次上传需重新合成

### 数据保存

- 所有用户数据保存在 `data/` 目录下
- 卸载程序时会清除 `data/` 目录，**重要数据请提前备份**
- 可以将整个 `data/` 文件夹拷贝到其他电脑实现数据迁移

### 手机端使用

- 手机和电脑必须连接**同一个 WiFi 网络**
- 扫码后如果无法访问，请检查电脑防火墙是否拦截了 5200 端口

---

# 开发者声明

本项目采用 [PolyForm Noncommercial License 1.0.0](LICENSE) 授权，允许个人学习、修改和非商业使用，禁止用于商业盈利目的。如不认可该条款,，则不能使用或引用软件包内任何代码和文件。详见根目录LICENSE。
请勿使用本项目制作、传播任何侵犯他人知识产权的内容。