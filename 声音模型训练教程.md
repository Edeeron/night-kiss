# 晚安吻 — 声音模型训练教程

本教程将指导你使用 GPT-SoVITS 训练自己的声音模型，以便在晚安吻中使用。

---

## 第一步：准备 GPT-SoVITS 引擎

GPT-SoVITS 引擎已附带在晚安吻文件中。

1. 找到 **GPT-SoVITS** 文件夹
2. 将其解压/复制到电脑上任意位置（建议路径**不要有中文**），例如：
   ```
   D:\GPT-SoVITS
   ```

> ⚠️ 请确保训练和推理使用同一份引擎，不要混用不同版本。

---

## 第二步：准备训练素材

训练素材是训练声音模型的**核心素材**，要求如下：

| 要求 | 说明 |
| --- | --- |
| 时长 | 建议总时长 **60 秒以上** |
| 格式 | **.wav** |
| 内容 | 目标说话人的清晰语音，吐字清晰 |
| 环境 | 安静、无背景音乐、无杂音 |
| 语速 | 正常语速，自然说话即可 |

---

## 第三步：启动 GPT-SoVITS 训练界面

1. 进入 GPT-SoVITS 文件夹
2. 双击 **`go-web.bat`** 启动训练界面
3. 等待命令行窗口显示类似以下提示：
   ```
   Running on local URL: http://127.0.0.1:9874
   ```
4. 浏览器会自动打开训练界面

> 💡 首次启动可能需要较长时间加载模型，请耐心等待。

---

## 第四步：训练模型

打开训练界面后，按以下顺序操作：

### 4.1 前置数据集处理

将你的原始录音处理成训练所需的格式：

1. **填写音频路径**：输入原始录音的完整路径
   ```
   例如：D:\audio\my_voice.wav
   ```
2. **填写音频文本**：输入参考音频中**实际说了什么**
   ```
   例如：大家好，欢迎来到我的频道
   ```
3. 点击 **「开启语音切分」** — 系统自动将音频切成小段
4. 点击 **「开启语音识别」** — 统自动识别音频内容并生成文本
5. 点击 **「开启音频标注」** — 文本校准，确认无误后点击提交

> ⚠️ 必须完成以上所有步骤后，才能进入训练阶段。如果跳过转录文本生成，训练会失败。

### 4.2 训练 SoVITS 和 GPT 模型

数据集处理完成后，切换到 **GPT-SoVITS-TTS** 模式：

- 在页面顶部填写**实验名称**，如 `myvoice`
- 选择与引擎一致的**模型版本**
- 参数按需调整

1. 点击 **「训练集格式化工具」**
2. 点击 **「开启文本分词与特征提取」「开启语音自监督提取」「开启语义Token提取」**
3. 点击 **「训练集格式化一键三连」**
4. 等待训练完成
5. 点击 **「微调训练」**
6. 点击 **「开启 SoVITS 训练」「开启 GPT 训练」**
7. 等待训练完成

> ⚠️ 训练时间取决于你的电脑配置（显卡）。

### 4.3 验收模型训练效果

1. 点击 **「推理」**
2. 点击 **「开启 TTS 推理」**
3. 上传参考音频及文本，输入要生成的文本，点击合成语音进行试听
3. 训练完成后，模型文件默认保存在：
   ```
   GPT-SoVITS\GPT_weights\实验名\
   ```

> ⚠️ 语音合成时间取决于你的电脑配置（显卡）。

---

## 第五步：找到训练好的模型文件

训练完成后，请确认文件所在位置以便管理：

| 文件 | 默认位置                             | 格式             |
|------|----------------------------------|----------------|
| **GPT 权重** | `GPT-SoVITS\GPT_weights\实验名\`    | `.ckpt` 或 `.pth` |
| **SoVITS 权重** | `GPT-SoVITS\SoVITS_weights\实验名\` | `.ckpt` 或 `.pth` |

> 💡 如果有多个 epoch 的文件，使用时推荐选择**数字最大**的（训练最充分）。
