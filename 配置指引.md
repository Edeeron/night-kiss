# 晚安吻 — 配置指引

晚安吻中一共存在六项配置：TTS引擎、GPT 权重、SoVITS 权重、参考音频、上传书籍、播客背景图。
本教程将指导你在晚安吻中配置 TTS引擎、GPT 权重、SoVITS 权重。

> 需要先完成声音模型训练，准备好 GPT 权重、SoVITS 权重和参考音频。

---

## 第一步：启动晚安吻

1. 双击桌面图标或 `run.bat` 启动晚安吻
2. 浏览器会自动打开 http://localhost:5200
3. 点击底部 **「配置」** 

---

## 第二步：上传模型文件

- **GPT 权重**：点击「选择文件」→ 选择训练好的 GPT `.ckpt` 文件
- **SoVITS 权重**：点击「选择文件」→ 选择训练好的 SoVITS `.ckpt` 文件

> 💡 模型文件默认位于 `GPT-SoVITS\GPT_weights\实验名\` 和 `GPT-SoVITS\SoVITS_weights\实验名\`。

---

## 第三步：上传参考音频

- **参考音频**：点击「选择文件」→ 选择原始录音 `.wav` 文件
  （系统会从中提取声音特征，建议使用清晰的 3~15 秒片段效果最佳）
- **参考音频文本**：在引擎中填写参考音频中**实际说了什么**
  ```
  例如：大家好，欢迎来到我的频道
  ```
- 点击 **「保存」**

---

## 第四步：配置引擎

- 在 **「引擎目录」** 填写 GPT-SoVITS 的路径：
  ```
  例如：D:\GPT-SoVITS
  ```
- 点击 **「保存」**
- 点击 **「启动引擎」**
- 等待提示「引擎已启动」
- 点击 **「测试连接」**
- 等待右上角显示「引擎就绪」


## 第五步：测试

- 确认引擎状态
- 上传 TXT 书籍
- 选择书籍 → 选择「播客模式」或「听书模式」
- 开始合成，享受定制朗读！

---

## 常见问题

| 问题        | 解决方法 |
|-----------| --- |
| 启动引擎失败    | 检查引擎目录是否正确 |
| 合成声音失真    | 确认训练和推理使用同一份引擎 |
| 合成声音重复/卡顿 | 检查参考音频文本是否填写正确 |
| 杀毒软件报毒    | 整合包可能误报，手动允许即可 |
| 端口被占用     | 关闭其他占用 9880 端口的程序，或重启电脑 |
